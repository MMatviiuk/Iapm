# 🚀 СТАРТ ЗАСТОСУНКУ - Prescription Clarity

## ⚡ Швидкий старт (30 секунд)

### Якщо ви вперше запускаєте проект:

```bash
npm install
npm run copy-db
npm run dev
```

### Якщо проект вже працював раніше:

```bash
npm run dev
```

---

## 🎯 Що має статися після запуску

1. **В терміналі ви побачите:**
```
  VITE v5.x.x  ready in XXX ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.x.x:5173/
```

2. **Автоматично відкриється браузер** (або відкрийте вручну: http://localhost:5173)

3. **Ви побачите Landing Page** з кнопками:
   - "Get Started" → Реєстрація
   - "Sign In" → Вхід

---

## 🔐 Demo Акаунти (готові до використання)

### Для тестування Patient (Пацієнт):
- **Email**: `patient@demo.com`
- **Password**: `demo123`
- **Роль**: Patient (для себе)

### Для тестування Caregiver (Опікун):
- **Email**: `caregiver@demo.com`
- **Password**: `demo123`
- **Роль**: Caregiver (опікун родичів)

### Для тестування Doctor (Лікар):
- **Email**: `doctor@demo.com`
- **Password**: `demo123`
- **Роль**: Healthcare Professional (лікар)

---

## 🧪 Швидка перевірка роботи

### Метод 1: Автоматичний тест бази даних

1. Запустіть проект: `npm run dev`
2. Відкрийте http://localhost:5173
3. Натисніть **F12** (відкрити DevTools)
4. Натисніть кнопку **"Debug"** (ліворуч внизу)
5. Натисніть **"🧪 Test Database"**

**Що має бути:**
- ✅ Зелена галочка "Database Loaded Successfully!"
- 5 Doctors
- 5 Caregivers
- 15 Patients

### Метод 2: Ручний тест логіну

1. Відкрийте http://localhost:5173
2. Натисніть **"Sign In"**
3. Введіть: `patient@demo.com` / `demo123`
4. Натисніть **"Sign In"**

**Що має бути:**
- Перенаправлення на Dashboard
- Показ аналітики (графіки, статистика)
- Бічна панель з навігацією (ліворуч на desktop)

---

## ❌ Що робити, якщо щось не так

### Помилка: "npm: command not found"

**Рішення**: Встановіть Node.js
```bash
# Завантажте Node.js з https://nodejs.org/
# Версія: 18.x або новіша
# Після встановлення перезапустіть термінал
```

### Помилка: "Cannot find module"

**Рішення**: Встановіть залежності
```bash
npm install
npm run copy-db
npm run dev
```

### Помилка: "Port 5173 is already in use"

**Рішення**: Інший процес використовує порт
```bash
# Windows
netstat -ano | findstr :5173
taskkill /PID <номер_процесу> /F

# macOS/Linux
lsof -ti:5173 | xargs kill -9
```

### Помилка: "Failed to load database"

**Рішення**: Скопіюйте базу даних
```bash
npm run copy-db
# Перезапустіть сервер (Ctrl+C, потім npm run dev)
```

### Помилка: Білий екран

**Рішення**: Очистіть кеш
```bash
# 1. Зупиніть сервер (Ctrl+C)

# 2. Видаліть кеш
rm -rf node_modules/.vite  # macOS/Linux
rmdir /s node_modules\.vite  # Windows

# 3. Перезапустіть
npm run dev
```

---

## 🔍 Діагностика проблем

### Перевірка 1: Node.js версія

```bash
node --version
# Має бути: v18.x.x або новіше
```

### Перевірка 2: Наявність файлів

```bash
# Перевірте, чи є база даних
ls -la data/complete-database.json  # macOS/Linux
dir data\complete-database.json     # Windows

# Має показати файл розміром ~100KB+
```

### Перевірка 3: Консоль браузера

1. Відкрийте http://localhost:5173
2. Натисніть **F12**
3. Перейдіть на вкладку **"Console"**
4. Подивіться на повідомлення

**Добре (норма):**
```
✓ Database loaded via direct import
✅ Demo users initialized
```

**Погано (помилка):**
```
❌ Failed to load database
Error: Cannot find module
```

### Перевірка 4: Network в DevTools

1. F12 → вкладка **"Network"**
2. Перезавантажте сторінку (F5)
3. Подивіться на запити

**Шукайте:**
- `complete-database.json` → має бути **200 OK** (або не буде запиту, якщо використовується direct import)

---

## 📊 Структура проекту

```
prescription-clarity/
├── data/
│   └── complete-database.json    ← База даних (15 пацієнтів)
├── public/
│   └── data/
│       └── complete-database.json ← Копія для production
├── components/
│   ├── Dashboard.tsx              ← Patient dashboard
│   ├── CaregiverDashboard.tsx    ← Caregiver dashboard
│   ├── DoctorDashboard.tsx       ← Doctor dashboard
│   └── ...
├── services/
│   └── api.ts                     ← Mock API (USE_MOCK_API = true)
└── App.tsx                        ← Головний компонент
```

---

## 🎓 Наступні кроки після запуску

### 1. Ознайомлення з інтерфейсом

**Для Patient (patient@demo.com):**
- [ ] Подивіться Dashboard з аналітикою
- [ ] Перейдіть на "Today" - розклад на сьогодні
- [ ] Подивіться "History" - історія прийому ліків
- [ ] Перейдіть в "Settings" - налаштування

**Для Caregiver (caregiver@demo.com):**
- [ ] Подивіться список Dependents (3 родичі)
- [ ] Перейдіть в Analytics - статистика по всіх
- [ ] Спробуйте додати нового dependent

**Для Doctor (doctor@demo.com):**
- [ ] Подивіться список Patients (4 пацієнти)
- [ ] Перейдіть в Analytics - когортна аналітика
- [ ] Подивіться Medication Database

### 2. Тестування функціоналу

- [ ] Додайте нове лікарство (Patient role)
- [ ] Відзначте прийом ліків (Patient role)
- [ ] Перегляньте аналітику (всі ролі)
- [ ] Спробуйте темний режим (Settings)
- [ ] Роздрукуйте розклад (Print Schedule)

### 3. Розробка

- [ ] Відкрийте код в VS Code
- [ ] Подивіться структуру компонентів
- [ ] Ознайомтеся з `Guidelines.md`
- [ ] Запустіть `npm run lint` для перевірки TypeScript

---

## 📖 Корисні файли

- **EMERGENCY_FIX_GUIDE.md** → Якщо щось зламалося
- **Guidelines.md** → Повна документація проекту
- **WEB_SAAS_TRANSFORMATION.md** → Опис трансформації в SaaS
- **README.md** → Загальна інформація

---

## 🔥 Часті проблеми та рішення

### Проблема: Сервер запустився, але не відкривається браузер

**Рішення**: Відкрийте вручну http://localhost:5173

### Проблема: Змінив код, але не бачу змін

**Рішення**: Vite має Hot Module Replacement (HMR)
- Зміни мають з'являтися автоматично
- Якщо не працює, перезавантажте сторінку (F5)

### Проблема: TypeScript помилки в консолі

**Рішення**: Більшість можна ігнорувати під час розробки
```bash
# Перевірка типів окремо
npm run lint
```

### Проблема: Не можу залогінитися

**Рішення**: Очистіть localStorage
```javascript
// В консолі браузера (F12)
localStorage.clear()
// Перезавантажте сторінку (F5)
```

---

## ✅ Чеклист успішного запуску

- [ ] Node.js v18+ встановлено
- [ ] Залежності встановлено (`npm install`)
- [ ] База даних скопійована (`npm run copy-db`)
- [ ] Сервер запущено (`npm run dev`)
- [ ] Браузер відкрито (http://localhost:5173)
- [ ] Landing page відображається
- [ ] Можна залогінитися з demo акаунтом
- [ ] Dashboard показується коректно
- [ ] Немає помилок в консолі (F12)

---

## 💡 Поради

1. **Завжди тримайте відкритим DevTools (F12)** - бачите помилки відразу
2. **Використовуйте Debug Panel** - швидка навігація між сторінками
3. **Тестуйте на різних ролях** - кожна роль має свій інтерфейс
4. **Зберігайте зміни в Git** - легко відкотитися, якщо щось зламалося
5. **Читайте Guidelines.md** - всі стандарти розробки там

---

**🎉 Успішного запуску!**

Якщо все працює - ви побачите професійний SaaS застосунок для управління ліками з повною підтримкою трьох ролей, аналітикою, досягненнями та адаптивним дизайном.

**Версія**: 2.0.0  
**Дата**: 5 листопада 2025  
**Автор**: https://github.com/MMatviiuk
