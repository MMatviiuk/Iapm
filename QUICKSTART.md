# 🚀 ШВИДКИЙ СТАРТ ДЛЯ НОВАЧКІВ

**Покрокова інструкція запуску додатку з нуля.**

---

## 📋 Що Потрібно Встановити (ОДИН РАЗ)

### 1. Node.js (JavaScript середовище)
**Windows/Mac:**
1. Відкрийте https://nodejs.org
2. Завантажте **LTS версію** (рекомендована)
3. Запустіть інсталятор → Next → Next → Install
4. Перевірка (відкрийте термінал):
   ```bash
   node --version  # Має показати v20.x.x
   npm --version   # Має показати 10.x.x
   ```

### 2. Git (система версій)
**Windows:**
1. https://git-scm.com/download/win
2. Завантажити → Запустити → Next (всі стандартні налаштування)

**Mac:**
```bash
# Відкрийте Terminal і введіть:
git --version
# Якщо не встановлено - Mac запропонує встановити
```

**Перевірка:**
```bash
git --version  # Має показати git version 2.x.x
```

### 3. VS Code (редактор коду) - ОПЦІОНАЛЬНО
1. https://code.visualstudio.com
2. Завантажити → Встановити
3. Рекомендовані розширення:
   - ESLint
   - Prettier
   - TypeScript and JavaScript

---

## 📦 Крок 1: Завантажити Код

### Варіант A: З GitHub (якщо публічний)
```bash
# 1. Відкрийте термінал (Terminal на Mac, PowerShell на Windows)
# 2. Перейдіть в папку де хочете зберігати проект:
cd Documents  # або інша папка

# 3. Клонуйте репозиторій:
git clone https://github.com/MMatviiuk/Iapm.git

# 4. Перейдіть в папку проекту:
cd Iapm
```

### Варіант B: Завантажити ZIP
1. https://github.com/MMatviiuk/Iapm
2. Code → Download ZIP
3. Розпакуйте в зручну папку
4. Відкрийте термінал в цій папці

---

## ⚙️ Крок 2: Встановити Залежності

```bash
# У папці проекту Iapm виконайте:
npm install

# Зачекайте 1-3 хвилини
# Має з'явитись папка node_modules
```

**Якщо помилка:**
- Переконайтесь що Node.js встановлений (`node --version`)
- Спробуйте: `npm install --legacy-peer-deps`

---

## 🔧 Крок 3: Налаштувати Змінні Оточення

### 1. Створити файл .env
```bash
# У папці проекту:
cp .env.example .env

# На Windows (якщо cp не працює):
copy .env.example .env
```

### 2. Відкрити .env файл і заповнити:
```bash
# Відкрийте .env в будь-якому текстовому редакторі
notepad .env      # Windows
open .env         # Mac
```

**Що заповнити (поки залиште так як є - працюватиме локально):**
```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_key_here
VITE_GOOGLE_VISION_API_KEY=your_key_here
VITE_OPENAI_API_KEY=sk-your_key_here
```

> **Примітка:** Додаток працюватиме БЕЗ цих ключів у demo-режимі!

---

## 🚀 Крок 4: Запустити Локально

```bash
npm run dev
```

**Має з'явитись:**
```
  VITE v6.3.5  ready in 523 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**Відкрийте браузер:** http://localhost:5173

**Демо акаунти для входу:**
- Email: `patient@test.com` | Password: `password123`
- Email: `caregiver@test.com` | Password: `password123`
- Email: `doctor@test.com` | Password: `password123`

---

## 📱 Крок 5: Зробити Android APK (ОПЦІОНАЛЬНО)

### Передумови:
1. **Android Studio** встановлено
   - https://developer.android.com/studio
   - Завантажити → Встановити (потребує 10-15 GB)

2. **Java JDK 17** встановлено
   ```bash
   java --version  # Має показати 17.x.x
   ```

### Процес:
```bash
# 1. Зібрати web версію
npm run build

# 2. Синхронізувати з Android
npx cap sync android

# 3. Відкрити в Android Studio
npx cap open android

# 4. У Android Studio:
# Build → Build Bundle(s) / APK(s) → Build APK(s)
# Зачекайте 2-5 хвилин

# 5. APK готовий:
# android/app/build/outputs/apk/debug/app-debug.apk
```

### Встановити на телефон:
```bash
# Підключіть телефон USB з налаштуванням "USB Debugging"
adb install android/app/build/outputs/apk/debug/app-debug.apk
```

---

## 🌐 Крок 6: Deploy на Vercel (ОПЦІОНАЛЬНО)

### 1. Створити акаунт Vercel
1. https://vercel.com → Sign Up
2. GitHub акаунт для входу

### 2. Deploy проект
1. New Project → Import Git Repository
2. Вибрати `MMatviiuk/Iapm`
3. **Framework Preset:** Vite
4. **Build Command:** `npm run build`
5. **Output Directory:** `build`
6. **Environment Variables:** Додати ключі з .env
7. Deploy → Готово!

**URL:** `https://your-project.vercel.app`

---

## 🗄️ Крок 7: Налаштувати Supabase (ОПЦІОНАЛЬНО)

### 1. Створити акаунт
1. https://supabase.com → Start your project
2. Sign up → GitHub акаунт

### 2. Створити проект
1. New Project
2. **Name:** `medication-tracker`
3. **Database Password:** Придумайте і збережіть!
4. **Region:** Ближчий до вас (Europe Central)
5. Create new project → Зачекайте 2 хв

### 3. Налаштувати БД
1. SQL Editor (ліва панель)
2. New Query
3. Відкрийте файл `supabase-schema.sql` з проекту
4. Скопіюйте ВЕСЬ вміст → Вставте в SQL Editor
5. RUN (Ctrl+Enter)
6. ✅ Success!

### 4. Отримати ключі
1. Settings → API
2. Скопіюйте:
   - **Project URL** → `.env` → `VITE_SUPABASE_URL`
   - **anon public** → `.env` → `VITE_SUPABASE_ANON_KEY`

### 5. Перезапустити додаток
```bash
# Зупинити (Ctrl+C)
npm run dev  # Запустити знову
```

---

## 🎥 Крок 8: Налаштувати Google Vision (ОПЦІОНАЛЬНО)

### 1. Google Cloud Console
1. https://console.cloud.google.com
2. Створити проект або вибрати існуючий

### 2. Активувати API
1. Navigation Menu → APIs & Services → Library
2. Знайти: **Cloud Vision API** → Enable
3. Знайти: **Video Intelligence API** → Enable

### 3. Створити API Key
1. APIs & Services → Credentials
2. Create Credentials → API Key
3. Скопіювати ключ → `.env` → `VITE_GOOGLE_VISION_API_KEY`

### 4. Обмежити ключ (безпека)
1. Edit API Key
2. Application restrictions → HTTP referrers
3. Add: `localhost:5173/*`, `your-vercel-domain.vercel.app/*`
4. API restrictions → Restrict key
5. Select: Cloud Vision API, Video Intelligence API
6. Save

---

## 🤖 Крок 9: Налаштувати OpenAI (ОПЦІОНАЛЬНО)

### 1. Створити акаунт
1. https://platform.openai.com
2. Sign up

### 2. Додати картку
1. Settings → Billing → Add payment method
2. **Мінімальний баланс:** $5

### 3. Створити API Key
1. API Keys → Create new secret key
2. Скопіювати → `.env` → `VITE_OPENAI_API_KEY=sk-...`

**Вартість:**
- ~$0.002 за консультацію
- 100 консультацій = $0.20

---

## 🐛 Вирішення Проблем

### Білд не запускається
```bash
# Очистити кеш і перевстановити:
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### Порт 5173 зайнятий
```bash
# Вбити процес на порту (Mac/Linux):
lsof -ti:5173 | xargs kill -9

# Windows PowerShell:
netstat -ano | findstr :5173
taskkill /PID [PID_NUMBER] /F
```

### API ключі не працюють
1. Перевірте що .env файл в кореневій папці проекту
2. Перезапустіть dev сервер (Ctrl+C → npm run dev)
3. Перевірте що ключі не мають пробілів

### Android Studio не відкривається
```bash
# Переконайтесь що Android Studio встановлено
# Спробуйте відкрити вручну:
# File → Open → папка проекту/android
```

---

## 📚 Корисні Команди

```bash
# Запустити локально
npm run dev

# Зібрати production версію
npm run build

# Синхронізувати з Android
npm run android:sync

# Відкрити Android Studio
npm run android:open

# Запустити на підключеному телефоні
npm run android:run

# Перевірити код
npm run lint  # якщо налаштовано

# Очистити все
rm -rf node_modules build android/app/build
npm install
```

---

## ✅ Чек-лист Готовності

- [ ] Node.js встановлено (`node --version`)
- [ ] Git встановлено (`git --version`)
- [ ] Код завантажено (`cd Iapm`)
- [ ] Залежності встановлено (`npm install`)
- [ ] .env файл створено (`cp .env.example .env`)
- [ ] Додаток запускається (`npm run dev`)
- [ ] Можу зайти http://localhost:5173
- [ ] Вхід працює (demo акаунти)

### Для Android:
- [ ] Android Studio встановлено
- [ ] Java JDK встановлено
- [ ] APK зібрано

### Для Production:
- [ ] Supabase налаштовано
- [ ] Google Vision API активовано
- [ ] Vercel deploy виконано
- [ ] Тести пройдені

---

## 🎓 Наступні Кроки

1. **Протестуйте функції:**
   - Додавання ліків
   - Відмітка прийому
   - Фото/відео сканер
   - Голосовий ввід

2. **Налаштуйте Supabase** (якщо потрібна синхронізація)

3. **Створіть Android APK** (якщо потрібен мобільний додаток)

4. **Deploy на Vercel** (якщо потрібен веб доступ)

---

## 💬 Допомога

**Документація:**
- `README.md` - Огляд проекту
- `SETUP.md` - Детальні налаштування
- `DEPLOY.md` - Інструкції deploy
- `MEDICATION_INVENTORY_SETUP.md` - Сканери

**Не працює?**
1. Перевірте всі кроки вище
2. Подивіться консоль браузера (F12)
3. Створіть issue на GitHub

---

**Успіхів! 🚀**
