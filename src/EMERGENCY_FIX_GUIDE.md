# 🆘 Emergency Fix Guide - Prescription Clarity

## Якщо нічого не працює - починайте звідси!

### 🎯 Швидкий старт (3 хвилини)

```bash
# 1. Зупиніть всі процеси (Ctrl+C в терміналі)

# 2. Встановіть залежності
npm install

# 3. Скопіюйте базу даних
npm run copy-db

# 4. Запустіть сервер
npm run dev
```

### 🧪 Перевірка бази даних

Після запуску сервера:

1. **Відкрийте браузер**: http://localhost:5173
2. **Натисніть F12** (відкрити DevTools)
3. **Натисніть кнопку "Debug"** (ліворуч внизу екрану)
4. **Натисніть "🧪 Test Database"**

Якщо тест пройшов успішно (зелена галочка ✅):
- База даних працює правильно
- Переходьте до тестування функціоналу

Якщо тест не вдався (червоний хрестик ❌):
- Дивіться інструкцію нижче

---

## 📋 Покрокова діагностика

### Крок 1: Перевірка Node.js та npm

```bash
node --version  # Має бути >= 18.0.0
npm --version   # Має бути >= 9.0.0
```

Якщо версія застаріла:
- Завантажте Node.js з https://nodejs.org/
- Перезавантажте термінал

### Крок 2: Очищення кешу

```bash
# Windows (PowerShell)
Remove-Item -Recurse -Force node_modules\.vite
npm run copy-db
npm run dev

# macOS/Linux
rm -rf node_modules/.vite
npm run copy-db
npm run dev
```

### Крок 3: Повне перевстановлення

Якщо попередні кроки не допомогли:

```bash
# 1. Видаліть залежності
rm -rf node_modules
rm package-lock.json

# 2. Встановіть заново
npm install

# 3. Скопіюйте БД
npm run copy-db

# 4. Запустіть
npm run dev
```

---

## 🔍 Перевірка конкретних проблем

### Проблема: "Cannot find module"

**Причина**: Відсутні залежності або зламаний кеш

**Рішення**:
```bash
npm ci  # Чиста встановка
npm run copy-db
npm run dev
```

### Проблема: "404 Not Found" для complete-database.json

**Причина**: База даних не скопійована в public/data/

**Рішення**:
```bash
# Перевірка наявності файлу
ls -la data/complete-database.json  # macOS/Linux
dir data\complete-database.json     # Windows

# Якщо файл існує, скопіюйте його
npm run copy-db

# Перевірте, чи з'явився файл
ls -la public/data/complete-database.json  # macOS/Linux
dir public\data\complete-database.json     # Windows
```

### Проблема: "Failed to load database"

**Причина**: JSON файл пошкоджено або неправильний формат

**Рішення**:
1. Відкрийте `/data/complete-database.json`
2. Переконайтеся, що він починається з `{` і закінчується `}`
3. Використайте JSON validator: https://jsonlint.com/
4. Якщо файл пошкоджено, відновіть з Git:
```bash
git checkout data/complete-database.json
npm run copy-db
npm run dev
```

### Проблема: Білий екран після логіну

**Причина**: Помилка в компонентах Dashboard

**Діагностика**:
1. Відкрийте консоль браузера (F12)
2. Подивіться на червоні помилки
3. Скопіюйте текст помилки

**Швидке рішення**:
```javascript
// В консолі браузера (F12)
localStorage.clear()
// Перезавантажте сторінку (F5)
```

### Проблема: TypeScript помилки при збірці

**Причина**: Несумісність типів

**Рішення**:
```bash
# Перевірка типів без запуску
npm run lint

# Якщо є помилки, подивіться на них в терміналі
# Більшість можна ігнорувати, якщо застосунок працює
```

---

## 🧪 Тестування функціоналу

### Тест 1: Реєстрація нового користувача

1. Перейдіть на http://localhost:5173
2. Натисніть "Get Started"
3. Заповніть форму:
   - Email: `test@example.com`
   - Password: `Password123!`
   - Name: `Test User`
   - Role: Patient
4. Натисніть "Create Account"

**Очікуваний результат**: Перенаправлення на onboarding

### Тест 2: Логін з demo акаунтом

1. Перейдіть на http://localhost:5173
2. Натисніть "Sign In"
3. Введіть:
   - Email: `patient@demo.com`
   - Password: `demo123`
4. Натисніть "Sign In"

**Очікуваний результат**: Dashboard з аналітикою

### Тест 3: Перегляд Caregiver dashboard

1. Залогіньтеся як `caregiver@demo.com` / `demo123`
2. Подивіться, чи відображаються dependents

**Очікуваний результат**: Список з 3 dependents

### Тест 4: Перегляд Doctor dashboard

1. Залогіньтеся як `doctor@demo.com` / `demo123`
2. Подивіться, чи відображаються patients

**Очікуваний результат**: Список з 4 patients

---

## 🔧 Відновлення після краху

Якщо **НІЧОГО** не працює і ви в розпачі:

```bash
# 1. Створіть резервну копію
cp -r . ../prescription-clarity-backup

# 2. Повний reset
git reset --hard HEAD
git clean -fd

# 3. Чиста встановка
npm ci

# 4. Скопіюйте БД
npm run copy-db

# 5. Запустіть
npm run dev
```

⚠️ **УВАГА**: Це видалить всі незбережені зміни!

---

## 📞 Де шукати допомогу

### 1. Перевірте консоль браузера (F12)

Помилки показують точну причину:
- `Cannot find module` → встановіть залежності
- `404 Not Found` → скопіюйте БД
- `TypeError` → перевірте типи

### 2. Перевірте термінал

Помилки компіляції показують файл і рядок:
```
Error: /components/Dashboard.tsx:42:15
```

### 3. Перевірте логи Vite

При запуску `npm run dev` дивіться на:
```
✓ Database loaded from public/data/complete-database.json
```

Якщо бачите:
```
⚠️ Source database file not found
```
→ Запустіть `npm run copy-db`

---

## ✅ Чеклист "Все працює"

- [ ] Сервер запускається без помилок
- [ ] База даних завантажується (✓ в консолі)
- [ ] Можна зареєструвати нового користувача
- [ ] Можна залогінитися з demo акаунтом
- [ ] Dashboard відображається
- [ ] Caregiver dashboard показує dependents
- [ ] Doctor dashboard показує patients
- [ ] Немає помилок в консолі браузера (F12)

---

## 🎯 Якщо все ще не працює

**Опишіть проблему детально:**

1. **Що ви робили?** (реєстрація, логін, перегляд сторінки)
2. **Що сталося?** (білий екран, помилка, краш)
3. **Який текст помилки?** (з консолі браузера або терміналу)
4. **Скріншот помилки** (якщо можливо)

**Надішліть:**
- Версію Node.js: `node --version`
- Версію npm: `npm --version`
- Операційну систему: Windows/macOS/Linux
- Текст помилки з консолі

---

## 🚀 Швидкі команди

```bash
# Запуск
npm run dev

# Перевірка TypeScript
npm run lint

# Збірка для production
npm run build

# Перегляд production збірки
npm run preview

# Копіювання БД
npm run copy-db

# Повна переустановка
rm -rf node_modules package-lock.json && npm install && npm run copy-db && npm run dev
```

---

## 📝 Нотатки

- **Mock API включено**: `USE_MOCK_API = true` в `/services/api.ts`
- **Demo акаунти**: patient@demo.com, caregiver@demo.com, doctor@demo.com
- **Пароль для всіх**: `demo123`
- **База даних**: 5 doctors, 5 caregivers, 15 patients
- **Порт**: http://localhost:5173

---

**Створено**: 5 листопада 2025  
**Версія**: 2.0.0  
**Автор**: https://github.com/MMatviiuk
